import 'package:flutter/material.dart';

class MealKitPage extends StatelessWidget {

  @override
    Widget build(BuildContext context) {
      return Column(
        children: [
          Flexible(
            child: Center(
              child: Text('(Meal-Kit) Work in Progress...'),
            ),
          ),
        ],
      );
    }
}