import 'package:flutter/material.dart';

class MartPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('(Mart) - Work In Progress...'),
    );
  }
}